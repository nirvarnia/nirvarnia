// //import xxx from './xxx.js'

// /**
//  * Fetch the value of a specific URL query parameter.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * getUrlQueryParam('Foo Bar')
//  * // → 'fooBar'
//  */
// const getUrlQueryParam = (val) => (
//   'TODO'
// )

// export default getUrlQueryParam



//     // $.getURLParameter
//     // Fetch the value of a specific query string parameter

//     $.getURLParameter = function (name) {
//         return decodeURIComponent((new RegExp('[?|&]' + name + '=' + '([^&;]+?)(&|#|;|$)').exec(window.location.search) || [, ''])[1].replace(/\+/g, '%20')) || null;
//     };


// // String = __.getUrlParameter(String)
// // Fetch the value of a specific query string parameter. Returns an empty string
// // for non-existent parameters or parameters with no value.

// __.getUrlParameter = function (name) {
//   return decodeURIComponent((new RegExp('[?|&]' + name + '=' + '([^&;]+?)(&|#|;|$)').exec(window.location.search) || [, ''])[1].replace(/\+/g, '%20')) || ''
// }

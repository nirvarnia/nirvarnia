
// // Object = __.getWindowScrol()
// // Returns the number of pixels that the window has been scrolled, vertically
// // and horizontally. The older page(X|Y)Offset API is included for better backwards
// // compatibility.

// __.getWindowScroll = function () {
//   return {
//     x: window.scrollX || window.pageXOffset || 0,
//     y: window.scrollY || window.pageYOffset || 0
//   }
// }

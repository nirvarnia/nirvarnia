# `forEach()`

Iterate through every element of the collection. Similar to each(), but the parameters for the iterator functions are different, and returning false from the iterator won’t stop the iteration.

This is a Zepto-provided method that is not part of the jQuery API. 


## Signature

TODO: Include standalone and $ wrapper signatures:

```txt
forEach(function(item, index, array){ ... }, [context]) 
```

#### Parameters

- **`arg1`** = `type` | `type`  \
  Description...

- **`arg2`** = `type` | `type` (default) \
  Optional. Description...

#### Throws

- `TypeError` - Missing or invalid parameters.

#### Returns

- `type` - Explanation.
- `type` - Explanation.


## Examples

```js
let rtn = fn(1, 2)
```


## Related

- [`xxxx()`](xxxx.md) - Description...
- [`xxxx()`](xxxx.md) - Description...

// //import xxx from './xxx.js'

// /**
//  * Queue a callback function to run when the HTML and all linked external
//  * resources are fully loaded in the browser.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * onFullLoad('Foo Bar')
//  * // → 'fooBar'
//  */
// const onFullLoad = (val) => (
//   'TODO'
// )

// export default onFullLoad



// // Void = __.onFullLoad(Function)
// // Queue a callback function to execute when the page has FULLY loaded, including all linked
// // external assets such as style sheets and images. Run the function immediately if already loaded.

// __.onFullLoad = function (fn) {
//   if (document.readyState === 'complete' || document.readyState === 'loaded') {
//     fn()
//   } else {
//     document.addEventListener('load', fn)
//   }
// }

/**
 * Creates a new, empty document fragment.
 *
 * @returns {Object} - `DocumentFragment` instance.
 * @example
 *
 * frag()
 * // → DocumentFragment
 */
const frag = () =>
  document.createDocumentFragment()

export { frag }

// //import xxx from './xxx.js'

// /**
//  * Inject into a container new HTML markup, replacing all prior contents.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * inject('Foo Bar')
//  * // → 'fooBar'
//  */
// const inject = (val) => (
//   'TODO'
// )

// export default inject




// // this = __(HTMLElements).Xxxx()
// //__.fn.inject = function(container) {

// //    if ( this.isElement() !== true ) {
// //        if ( this.isString() !== true ) {
// //            throw new TypeError( 'Expect DOM element node or HTML string' )
// //        }
// //    }

// //    container = __( container )

// //    if ( container.isElement() !== true ) {
// //        throw new TypeError( 'Expect DOM element node' )
// //    }

// //    if ( this.isString() === true ) {
// //        container.value.innerHTML = this._targets
// //    }
// //    else if ( this.isElement() === true ) {
// //        container.empty()
// //        container.value.appendChild( this._targets )
// //    }

// //    return this

// //}


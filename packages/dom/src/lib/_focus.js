// //import xxx from './xxx.js'

// /**
//  * Move the keyboard focus to a particular HTML element.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * focus('Foo Bar')
//  * // → 'fooBar'
//  */
// const focus = (val) => (
//   'TODO'
// )

// export default focus


// // this = __(HTMLElements).focus()
// // Move focus to the first HTMLElement in the collection, if it does not
// // already have focus.

// __.fn.focus = function () {
//   if (this._targets[0] && this._targets[0] !== document.activeElement) {
//     this._targets[0].focus()
//   }
//   return this
// }

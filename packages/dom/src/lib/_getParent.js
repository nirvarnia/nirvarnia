// //import xxx from './xxx.js'

// /**
//  * Returns the immediate parent element nodes of a given DOM node.
//  *
//  * Returns ``null`` if the first argument is not a DOM node, or if it does
//  * not have a parent — i.e. if it is the document itself, or if the node is
//  * not currently included in the document.
//  *
//  *     node|null = getParent(node)
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * getParent('Foo Bar')
//  * // → 'fooBar'
//  */
// const getParent = (val) => (
//   'TODO'
// )

// export default getParent




    //     /*
    //     node|null getParent( node )
    //     ---------------------------
    //     Description
    //         Returns the immediate parent element of a given node.
    //     Arguments
    //         DOM element reference.
    //     Returns
    //         DOM node reference.
    //         Null if no parent found.
    //     Notes
    //         el.parentNode behaves consistently in all modern browsers.
    //     */
    //     getParent: function( target ) {
    //         var _parent = target.parentNode;
    //         return _parent;
    //     },


// // this = __(HTMLElements).Xxxx()

// //__.fn.getParent = function() {
// //    if ( this._targets.length > 1 ) {
// //        throw new Error( 'Interface.Elements.getParents() requires a single element.' )
// //    }
// //    //if ( __.isDocument( this._targets[0] ) ) {
// //    //    throw new Error( 'Document does not have parent elements.' )
// //    //}
// //    var parent = this._targets[0].parentNode
// //    if ( parent === null ) {
// //        throw new Error( 'No parent element found.' )
// //    }
// //    return new __( parent )
// //}

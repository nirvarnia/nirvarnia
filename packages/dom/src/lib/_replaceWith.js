// //import xxx from './xxx.js'

// /**
//  * Swap one or more HTML elements for some new ones.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * replaceWith('Foo Bar')
//  * // → 'fooBar'
//  */
// const replaceWith = (val) => (
//   'TODO'
// )

// export default replaceWith





// // this = __(HTMLElements).Xxxx()

// //__.fn.replaceWith = function() {

// //    var _parent = I.Doc.getParent( remove )
// //    var _result = false
// //    if ( _parent ) {
// //        _parent.replaceChild( add, remove )
// //        _result = true
// //    }
// //    return _result

// // ***

// //    if ( this.isElement() !== true ) {
// //        throw new TypeError( 'Expect DOM element node' )
// //    }
// //    if ( this.isRoot() === true  ||  this.isBody() === true ) {
// //        throw new TypeError( 'Cannot replace the <body> or <html> elements' )
// //    }
// //    if ( target.hasParents() !== true ) {
// //        throw new TypeError( 'Cannot replace an element that is not part of the DOM' )
// //    }
// //    replacement = __( replacement )
// //    if ( replacement.isElement() !== true ) {
// //        throw new TypeError( 'Expect DOM element node' )
// //    }
// //    this.parent().value.replaceChild( replacement.value, this._targets )
// //    return this

// //}

// //import xxx from './xxx.js'

// /**
//  * Generate an array representing all of the elements that sit adjacent in the
//  * DOM tree to another element.
//  *
//  *     array = getSiblings(node)
//  *
//  * As with a number of the DOM traversal methods, there is no native
//  * replacement for jQuery's ``siblings()``, so we'll need to replace it with
//  * a method of our own.
//  *
//  * @param {string} [val=''] Description.
//  * @returns {string} Description.
//  * @example
//  *
//  * getSiblings('Foo Bar')
//  * // → 'fooBar'
//  */
// const getSiblings = (val) => (
//   'TODO'
// )

// export default getSiblings


// // this = __(HTMLElements).Xxxx()

// //__.fn.getSiblings = function() {

// //    if ( this.isElement() !== true ) {
// //        throw new TypeError( 'Expect DOM element node' )
// //    }
// //    if ( ! this._targets.parentNode ) {
// //        throw new Error( 'Element has no parents or is not included in the DOM tree' )
// //    }
// //    var _i = 0
// //    var _sibling = this._targets.parentNode.firstChild
// //    var _matches = []
// //    while ( _sibling ) {
// //        if ( _sibling.nodeType === 1  &&  _sibling !== this._targets ) {
// //            _matches.push( __( _sibling ) )
// //        }
// //        _sibling = _sibling.nextSibling
// //    }
// //    if ( fn ) {
// //        _matches.forEach( function ( element, index, array ) {
// //            fn.call( element )
// //        } )
// //    }
// //    return __( _matches )

// //}
